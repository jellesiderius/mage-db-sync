import MainTasks from './mainTasks'
import * as fs from 'fs'

class DownloadTask {
    configure = async (list, config) => {
        console.log(this.config);
        
        return list;
    }
}

export default DownloadTask